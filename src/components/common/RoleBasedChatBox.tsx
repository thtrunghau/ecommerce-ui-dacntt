import React from "react";
import ChatBox from "./ChatBox";

// Hiển thị chatbox cho mọi user, kể cả guest
const RoleBasedChatBox: React.FC = () => {
  return <ChatBox />;
};

export default RoleBasedChatBox;
